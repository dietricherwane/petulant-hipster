<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />

  <title>Quick Win::Renvoyer les instructions de confirmation</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>

<body class="nobg loginPage">

<!-- Top fixed navigation -->
<div class="topNav">
    <div class="wrapper">
        <div class="userNav">
            <ul>
                <li></li>
            </ul>
        </div>
        <div class="clear"></div>
    </div>
</div>



<!-- Main content wrapper -->
<div class="loginWrapper">
  <% flash.each do |key, value| %>
    <div class="nNote nInformation hideit" style = "width: 340px;">
      <p>
        <%= value %>
      </p>
    </div>
  <% end %>
  <%= devise_error_messages! %>

  <div class="loginLogo"><img src="<%= asset_path 'loginLogo.png' %>" alt="" /></div>
  <div class="widget">
      <div class="title"><img src="<%= asset_path 'icons/dark/files.png' %>" alt="" class="titleIcon" /><h6>Réinitialiser le mot de passe</h6></div>
      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
        <fieldset>
            <div class="formRow">
              <label for="login">Email:</label>
              <div class="loginInput">
                <%= f.text_field :email, autofocus: true, placeholder: "Veuillez entrer votre email" %>
              </div>

              <div class="clear"></div>
            </div>

            <div class="loginControl">
              <%= f.submit "Renvoyer l'email de confirmation", class: "dredB logMeIn" %>
              <div class="clear"></div>
            </div>
        </fieldset>
      <% end %>

      <%= render "devise/shared/links" %>
  </div>
</div>

</body>
</html>
